% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delta_energy.R
\name{delta_energy}
\alias{delta_energy}
\alias{ddg_dv}
\alias{ddg_tds}
\alias{delta_energy_dvs}
\alias{ddgact_dv}
\alias{ddgact_tds}
\title{Calculate energy differences between a mutant and wild type}
\usage{
ddg_dv(wt, mut)

ddg_tds(wt, mut, beta = beta_boltzmann())

delta_energy_dvs(wt, mut, ideal = wt)

ddgact_dv(wt, mut, ideal = wt, pdb_site_active = NA)

ddgact_tds(wt, mut, ideal = wt, pdb_site_active = NA, beta = beta_boltzmann())
}
\arguments{
\item{wt}{A protein object}

\item{mut}{A second protein}

\item{beta}{Inverse temperature, \code{1/kT}}

\item{ideal}{A protein object whose conformation defines the reference (ideal) state.
Defaults to \code{wt}.}

\item{pdb_site_active}{A vector of active-site residues, in pdb numbering (\code{resno})}
}
\value{
A (scalar) energy difference between mutant and wild type.
}
\description{
Calculate energy differences between a mutant and wild type
}
\details{
`ddg_dv` calculates the minimum-energy difference between \code{mut} and \code{wt}

`ddg_tds` calculates the entropic free energ difference between \code{mut} and \code{wt}

`delta_energy_dvs` calculates the ideal-conformation stress-energy difference between \code{mut} and \code{wt}

`ddgact_dv` calculates the energy contribution to the change in activation energy between \code{mut} and \code{wt}

`ddgact_tds` calculates the entropy contribution to the change in activation energy between \code{mut} and \code{wt}
}
\examples{
wt <- set_enm(pdb_2acy_A, node = "ca", model = "ming_wall",
              d_max = 10.5, frustrated = FALSE)
mut <- get_mutant_site(wt, site_mut = 11, mutation = 1, seed = 1024)

ddg_dv(wt, mut)      # minimum-energy difference

# Under "lfenm" the contact map — and so kmat and its spectrum — is unchanged,
# which makes the entropic term exactly zero. It is informative only for models
# that rebuild the network, such as "sclfenm".
ddg_tds(wt, mut)

}
\seealso{
[delta_structure_by_site] and [delta_motion_by_site] for per-site
  profiles rather than scalars. [get_mutant_site()] produces the `mut` argument;
  [set_enm()] the `wt`.
}
