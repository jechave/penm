% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delta_motion_by_site.R
\name{delta_motion_by_site}
\alias{delta_motion_by_site}
\alias{delta_motion_dmsfi}
\alias{delta_motion_dbhati}
\alias{delta_motion_rwsipi}
\alias{delta_motion_dhi}
\title{Compare the motions of two proteins site by site}
\usage{
delta_motion_dmsfi(wt, mut)

delta_motion_dbhati(wt, mut)

delta_motion_rwsipi(wt, mut)

delta_motion_dhi(wt, mut)
}
\arguments{
\item{wt}{A protein object with \code{enm} defined}

\item{mut}{A second protein object  with \code{enm} defined}
}
\value{
A vector \code{(x_i)} of size \code{nsites}, where \code{x_i} is the property compared, for site i.
}
\description{
Given two proteins, compare their conformational ensembles (fluctuation patterns),
the proteins'  \code{cmat}, and normal modes (Principal components) are assumed known.
}
\details{
(This version works only for wt and mut with no indels)

`delta_motion_dmsfi` returns site-dependent profile of changes of mean-square fluctuations \eqn{\delta \sigma_i^2}

`delta_motion_dbhati` returns site-dependent profile of dbhat distances

`delta_motion_rwsipi` returns site-dependent profile of rwsip similarity

`delta_motion_dhi` returns site-dependent profile of dh similarity
}
\examples{
wt <- set_enm(pdb_2acy_A, node = "ca", model = "ming_wall",
              d_max = 10.5, frustrated = FALSE)
mut <- get_mutant_site(wt, site_mut = 11, mutation = 1, seed = 1024)

dmsfi <- delta_motion_dmsfi(wt, mut)
length(dmsfi)                       # one value per site

# Note these are all zero here: "lfenm" perturbs equilibrium edge lengths but
# leaves kmat — and hence cmat and the normal modes — untouched, so the motion
# is unchanged by construction. Use a model that rebuilds the network to see a
# non-zero profile.
range(dmsfi)

}
\seealso{
[delta_motion_by_mode] for the same comparisons resolved by normal mode
  rather than by site. [delta_structure_by_site] compares structure rather than
  motion. [get_mutant_site()] produces the `mut` argument; [set_enm()] the `wt`.
}
